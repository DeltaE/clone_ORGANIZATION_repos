# config.env
# Configuration for clone_org_repos.bash
# Example:
# API_URL=https://api.github.com/orgs
# ORG_NAME=DeltaE
# GITHUB_TOKEN=your_token  # Optional: GitHub token for private repos and higher API rate limits
#                          # For private repos, token needs 'repo' scope or organization access
# -----------------------------------

API_URL=https://api.github.com/orgs
ORG_NAME=DeltaE
MASTER_FOLDER=cloned_repos
